// JavaScript код для игры
const startButton = document.querySelector('.start-button');
const bonusButton = document.querySelector('.bonus-button');
const achievementButton = document.querySelector('.achievement-button');
const exitButton = document.querySelector('.exit-button');

startButton.addEventListener('click', () => {
  // Код для начала игры
});

bonusButton.addEventListener('click', () => {
  // Код для запуска бонуса
});

achievementButton.addEventListener('click', () => {
  // Код для проверки достижений
});

exitButton.addEventListener('click', () => {
  // Код для выхода из игры
});
